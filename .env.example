# Example environment file for Viht bot
# This file lists keys used by the bot. Do NOT place real secrets in the repository.
# Create a private `.env` on your server and set the required values there.

# Discord and application settings
DISCORD_TOKEN=
CLIENT_ID=
GUILD_ID=

# Channel / role IDs (leave empty to use defaults configured in code)
WELCOME_CHANNEL_ID=
AI_CHAT_CHANNEL_ID=
ANNOUNCE_CHANNEL_ID=
MUSIC_LOG_CHANNEL_ID=

# Feature flags (true/false)
USE_MOCK_AI=false
GUILD_MEMBERS_INTENT=false
MESSAGE_CONTENT_INTENT=true

# AI provider key (Gemini / Google)
GEMINI_API_KEY=

# Optional: Supabase (if used)
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# IMPORTANT: keep your real `.env` private and do not commit it to the repository.
