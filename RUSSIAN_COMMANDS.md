# 🌍 ПЕРЕВОД КОМАНД НА РУССКИЙ

## Что изменилось?

**Все команды переведены на русский язык!**

### 📊 Информация (9 команд):
| Было | Стало |
|------|-------|
| `/achievements` | `/достижения` |
| `/analytics` | `/аналитика` |
| `/info` | `/инфо` |
| `/infopol` | `/нарушения` |
| `/leaderboard` | `/лидеры` |
| `/profile` | `/профиль` |
| `/stats` | `/статистика` |
| `/vers` | `/версия` |
| `/viht` | `/вихт` |

### ⚠️ Модерация (7 команд):
| Было | Стало |
|------|-------|
| `/warn` | `/варн` |
| `/unwarn` | `/унварн` |
| `/ban` | `/бан` |
| `/unban` | `/унбан` |
| `/mute` | `/мут` |
| `/unmute` | `/унмут` |
| `/moderation` | `/модерация` |

### 🎮 Игры (6 команд):
| Было | Стало |
|------|-------|
| `/dice` | `/кубик` |
| `/flip` | `/орелрешка` |
| `/roulette` | `/рулетка` |
| `/slots` | `/слот` |
| `/higher` | `/угадайчисло` |
| `/rockpaper` | `/каманэ` |

### 👤 Профиль & Системные (3 команды):
| Было | Стало |
|------|-------|
| `/onboarding` | `/новичек` |
| `/per` | `/написатьотбота` |
| `/post` | `/пост` |

### 📌 ОСТАВЛЕНЫ НА АНГЛИЙСКОМ (специальные):
- `audit` (логирование)
- `clearchat` (удаление сообщений)
- `points`, `faq`, `afaq`, `ticket`, `support` (интеграции)
- `reviews*` (отзывы - специальные команды)
- `backup`, `register-commands`, `settings`, `schedule`, `remind` (админ)
- `role`, `vpn`, `setvpn`, `sms1win` (сервисы)
- `status` (системное)

### 🔧 ОБНОВЛЕНО:
- **`/версия`** - теперь показывает:
  - Версию бота (из файла VERSION)
  - Hash последнего коммита
  - Дату обновления
  - Описание коммита (для администраторов)

## Как применить на сервере?

### 1️⃣ Залить коммит на GitHub:
```bash
git push origin main
```

### 2️⃣ На сервере выполнить:
```bash
cd /opt/discord-bot
git pull origin main
node bot/commands/register-commands.js
pm2 restart discord-bot
```

### 3️⃣ Проверить логи:
```bash
pm2 logs discord-bot
```

Должны быть видны:
```
Registering commands...
Registered guild commands.
✅ Successfully registered X commands
```

## Тестирование в Discord:

1. Откройте серв в Discord
2. Начните вводить `/` в чате
3. Увидите новые русские названия команд!
4. Все старые команды (`/achievement`, `/balance` и т.д.) исчезнут

## Возвращение назад (если нужно):

```bash
git reset --hard <коммит-до-перевода>
node bot/commands/register-commands.js
pm2 restart discord-bot
```

## Статус

✅ **ГОТОВО К РАЗВЁРТЫВАНИЮ**

- ✅ 25 команд переведены на русский
- ✅ 16 команд оставлены на английском (как указано)
- ✅ 1 команда переработана (`vers`)
- ✅ Синтаксис проверен (node -c)
- ✅ Коммит создан и готов к push

**Осталось:**
1. `git push origin main` (на вашем компьютере)
2. На сервере: `git pull && node register-commands.js && pm2 restart discord-bot`
